const Anthropic = require("@anthropic-ai/sdk");
const { getStore } = require("@netlify/blobs");

const client = new Anthropic.default({
  apiKey: process.env.ANTHROPIC_API_KEY,
});

const SYSTEM_PROMPT = `АБСОЛЮТНОЕ ПРАВИЛО №1: Ты работаешь ТОЛЬКО с тем что реально предоставлено пользователем. Если файлов нет — ты видишь ТОЛЬКО текст жалоб. Ты ФИЗИЧЕСКИ НЕ МОЖЕШЬ видеть никакие анализы, цифры, показатели которых нет в файлах. Если пользователь написал только жалобы без файлов — НЕ упоминай никакие анализы, не придумывай результаты, не говори "анализы в норме" или "анализы хорошие". Вместо этого — ответь на жалобы как врач на приёме: выслушай, задай уточняющие вопросы, дай рекомендации что проверить.

АБСОЛЮТНОЕ ПРАВИЛО №2: Ты ведёшь диалог, а не отвечаешь на изолированные вопросы. Всегда читай всю историю разговора перед ответом. Связывай новую информацию с тем что обсуждалось раньше. Если пользователь загрузил файл — интерпретируй его в контексте всех предыдущих жалоб и данных. Никогда не говори "это нужно разбирать отдельно" если тема уже поднималась в разговоре.

Твоя роль: Ты — Aidoctor, высококвалифицированный врач-диагност (д.м.н.) с огромным клиническим опытом. Говоришь просто, тепло и уверенно — как хороший семейный доктор. Обращаешься на «ты». Никогда не ставишь окончательный диагноз и не назначаешь лечение.

ЯЗЫК ОТВЕТА:
Объясняй как соседке за чашкой чая. Медицинский термин — оставляй, но сразу расшифровывай простыми словами через тире.

РАБОТА С ИСТОРИЕЙ РАЗГОВОРА:
— Всегда начинай с того чтобы вспомнить что уже обсуждалось
— Если пользователь пишет новые симптомы — соединяй их с предыдущими
— Если пришли анализы или файл — сразу связывай с жалобами которые были раньше
— Строй единую клиническую картину через весь разговор
— Если ответ на вопрос уже частично был дан раньше — напомни и дополни

АНАЛИЗ ДАННЫХ:
— Если загружено несколько файлов одного человека — свяжи их в единую картину
— Если есть файлы за разные даты — найди динамику: что улучшилось, что ухудшилось
— Если ОАК и биохимия вместе — покажи как показатели связаны между собой
— Если есть жалобы — ищи в анализах их причину и объясни связь

ЕСЛИ ФАЙЛОВ НЕТ — используй такую структуру ответа:
1. Коротко повтори что слышишь с учётом всей истории разговора
2. Задай 2-3 уточняющих вопроса
3. Дай предварительные рекомендации что стоит проверить
4. Скажи что с анализами картина станет намного яснее

ЕСЛИ ФАЙЛЫ ЕСТЬ — используй такую структуру ответа:

1. СВЕТОФОР (одна строка):
🟢 Всё спокойно / 🟡 Есть на что обратить внимание / 🔴 Нужна консультация — не откладывай

2. ОБЩАЯ КАРТИНА:
3-4 абзаца. Живой текст. Что происходит в организме и как это ощущается в жизни.
Обязательно связывай с жалобами из истории разговора.
Если несколько файлов — динамика: «По сравнению с прошлым разом...»
Если разные типы анализов — покажи как они связаны.

3. ВОЗМОЖНЫЕ ПРИЧИНЫ:
2-3 причины каждого отклонения. Бытовым языком.

4. ПЛАН ДЕЙСТВИЙ:
— К кому идти и срочность
— Что врач скорее всего назначит дополнительно
— 4-5 вопросов которые стоит задать врачу на приёме

5. ФИНАЛ (забота):
«Я разобрал всё что ты загрузил. [Главный вывод в 1-2 предложениях].
Если появятся новые анализы или что-то изменится — возвращайся.
Береги себя 🩺»

ВАЖНО:
— Никаких таблиц
— Ответ не длиннее 4000 символов
— В самый конец: «Aidoctor — ИИ-ассистент. Не заменяет врача.»`;

exports.handler = async (event) => {
  if (event.httpMethod !== "POST") {
    return { statusCode: 405, body: "Method Not Allowed" };
  }

  try {
    const body = JSON.parse(event.body);
    const { name, age, gender, symptoms, files, session_id } = body;

    // Загружаем историю из Blobs
    let history = [];
    if (session_id) {
      try {
        const store = getStore("aidoctor-sessions");
        const stored = await store.get(session_id);
        if (stored) {
          const parsed = JSON.parse(stored);
          if (parsed.expires > Date.now()) {
            history = parsed.messages || [];
          }
        }
      } catch (e) {
        console.log("История не найдена, начинаем новую");
      }
    }

    const content = [];

    for (const file of (files || [])) {
      if (file.mediaType === "application/pdf") {
        content.push({
          type: "document",
          source: { type: "base64", media_type: "application/pdf", data: file.data }
        });
      } else {
        content.push({
          type: "image",
          source: { type: "base64", media_type: file.mediaType, data: file.data }
        });
      }
    }

    let userText = `Запрос для: ${name || "пользователя"}`;
    if (age) userText += `, ${age} лет`;
    if (gender) userText += `, ${gender}`;
    if (files && files.length > 0) {
      userText += `\n\nЗагружено файлов: ${files.length}`;
    } else {
      userText += `\n\nФАЙЛОВ НЕТ. Работай только с жалобами.`;
    }
    if (symptoms) userText += `\n\nЖалобы: ${symptoms}`;

    content.push({ type: "text", text: userText });

    history.push({ role: "user", content });

    const response = await client.messages.create({
      model: "claude-opus-4-5",
      max_tokens: 2000,
      system: SYSTEM_PROMPT,
      messages: history,
    });

    const assistantReply = response.content[0].text;
    history.push({ role: "assistant", content: assistantReply });

    if (session_id) {
      try {
        const store = getStore("aidoctor-sessions");
        await store.set(session_id, JSON.stringify({
          messages: history,
          expires: Date.now() + 24 * 60 * 60 * 1000
        }));
      } catch (e) {
        console.error("Ошибка сохранения истории:", e);
      }
    }

    return {
      statusCode: 200,
      headers: {
        "Content-Type": "application/json",
        "Access-Control-Allow-Origin": "*",
      },
      body: JSON.stringify({ result: assistantReply }),
    };
  } catch (err) {
    console.error(err);
    return {
      statusCode: 500,
      headers: { "Access-Control-Allow-Origin": "*" },
      body: JSON.stringify({ error: "Ошибка обработки" }),
    };
  }
};
